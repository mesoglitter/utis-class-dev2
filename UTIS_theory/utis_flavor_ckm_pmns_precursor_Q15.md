# UTIS Flavor / CKM / PMNS Precursor Q-15

## 1. Goal

Construct a precursor scaffold for flavor mixing emerging from branch-vacuum geometry.

---

## 2. Core interpretation

Current UTIS idea:

```text
mixing angles
← branch-vacuum overlap geometry
```

Flavor structure is interpreted as:

```text
localized vs delocalized branch states
```

---

## 3. Branch-overlap kernel

```text
Overlap(i,j) ~ exp[-distance(branch_i, branch_j)]
```

Interpretation:

```text
nearby branch states -> large mixing
distant branch states -> suppressed mixing
```

---

## 4. CKM-like hierarchy scaffold

```text
Matrix([[1, epsilon_q, epsilon_q**3], [epsilon_q, 1, epsilon_q**2], [epsilon_q**3, epsilon_q**2, 1]])
```

Interpretation:

```text
small off-diagonal overlap
localized branch vacuum structure
hierarchical mixing
```

---

## 5. PMNS-like hierarchy scaffold

```text
Matrix([[1, epsilon_l, epsilon_l], [epsilon_l, 1, epsilon_l], [epsilon_l, epsilon_l, 1]])
```

Interpretation:

```text
large overlap structure
delocalized branch geometry
large flavor mixing
```

---

## 6. Possible UTIS interpretation

### localized_branch

```text
small overlap -> CKM-like hierarchy
```

### delocalized_branch

```text
large overlap -> PMNS-like mixing
```

### folded_sector

```text
screened localized vacuum branch
```

### unfolded_sector

```text
extended propagation branch
```

### vacuum_overlap

```text
mixing generated by branch-state overlap
```

### cyclic_geometry

```text
mixing structure sourced by vacuum ordering
```

---

## 7. Physical intuition

```text
folded/screened branch states
→ localized vacuum modes
→ small overlap
→ CKM-like hierarchy

unfolded/extended branch states
→ broad vacuum support
→ large overlap
→ PMNS-like mixing
```

---

## 8. Important caution

At this stage:

```text
This is NOT yet a derived flavor theory.
```

Still required:

```text
exact vacuum wavefunctions
unitary mixing derivation
mass hierarchy generation
CP phase structure
gauge embedding
fermion-sector consistency
```

---

## 9. Long-term target

Desired chain:

```text
branch vacuum geometry
→ overlap structure
→ effective flavor basis
→ mixing matrices
→ CKM / PMNS emergence
```

---

## 10. Current status

Current UTIS level:

```text
precursor scaffold only
```

but already suggests:

```text
mixing geometry may emerge from
vacuum-order / branch-overlap structure.
```

---

## 11. Next step Q-16

Construct explicit vacuum-wavefunction overlap model:

```text
branch wavefunctions
vacuum localization width
overlap integrals
unitary normalization
emergent mixing-angle structure
```
